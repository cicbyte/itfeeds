
# CLI tool, only in development environment.
# https://goframe.org/docs/cli
gfcli:
  gen:
    dao:
      - link: "mysql:root:hasd721ba9@tcp(192.168.31.93:39507)/rss_db"
        descriptionTag: true
  packSrc: "resource/public,resource/sql"

  docker:
    build: "-a amd64 -s linux -p temp -ew"
    tagPrefixes:
      - my.image.pub/my-app